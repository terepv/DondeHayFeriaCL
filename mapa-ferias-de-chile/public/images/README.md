# Site images

Drop your logo here (e.g. `logo.png` or `logo.svg`). It will be served at `/images/logo.png` (or whatever filename you use).

The header logo uses `/images/favicon.png` — use a filename without spaces (e.g. `favicon.png`).
